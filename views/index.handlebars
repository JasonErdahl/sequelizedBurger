<div class="container">
  <div class="jumbotron jumbotron-billboard">
    <div class="img"></div>
    <h1><span class="badge badge-secondary">Eat-Da-Burger!</span></h1>
    <hr>    
    <div class="jumbotron-description">
      <p>Eat-Da-Burger! is a restaurant app that lets users input the names of burgers they'd like to eat.</p>
      <p>Whenever a user submits a burger's name, your app will display the burger on the left side of the page -- waiting to be devoured.</p>
      <p>Each burger in the waiting area also has a Devour it! button. When the user clicks it, the burger will move to the right side of the page.</p>
      <p>Your app will store every burger in a database, whether devoured or not.</p>
    </div>
    <hr>
  </div>
  <div class="row">
    <div class="col-lg-6">
      <div class="card">
        <div class="card-header">
          <h3 class="card-title"><strong>Waiting to be devoured!</strong></h3>
        </div>
        <div class="card-body" id="well-section">
          <!-- Content will go in here -->
          <ul>
            {{#each burgers}}
              {{#unless devoured}}
                {{> burgers/burger-block devour=true}}
              {{/unless}}
            {{/each}}
          </ul>
        </div>
      </div>
    </div>	
    <div class="col-lg-1">
      <!-- Space -->
    </div>
    <div class="col-lg-5">
      <div class="card">
        <div class="card-header">
          <h3 class="card-title"><strong>Burgers that were devoured!</strong></h3>
        </div>
        <div class="card-body" id="well-section">
          <!-- Content will go in here -->
          <ul>
            {{#each burgers}}
              {{#if devoured}}
                {{> burgers/burger-block devour=false}}
              {{/if}}
            {{/each}}
          </ul>
          <!-- Content will go in here -->
        </div>
      </div>
    </div>		
  </div>
  <div class="row">
    <div class="col-lg-12">
      <!-- Space -->
      <p>&nbsp;</p>
    </div>
  </div>
  <div class="row newBurgerWrapper">
    <div class="col-lg-12">
      <div class="card">
        <div class="card-header">
          <h3 class="card-title"><strong>Add a Burger</strong></h3>
        </div>
        <div class="card-body" id="well-section">
          <!-- Content will go in here -->
          <form class="create-form">

            <div class="form-group">
              <label for="de">Burger Name:</label>
              <input type="text" id="de" name="burger_name">
            </div>

            <div class="form-group">
              <label for="devoured">Devoured?</label><br>
              <input type="radio" name="devoured" value="1" checked> devoured!<br>
              <input type="radio" name="devoured" value="0"> Waiting
            </div>

            <button type="submit">Add Burger</button>
          </form>
          <!-- Content will go in here -->
        </div>
      </div>
    </div>		
  </div>
</div>
